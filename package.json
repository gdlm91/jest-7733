{
  "name": "jest-broken",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "test:workaround": "jest --config jestworkaround.config.js"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "lerna": "^3.15.0",
    "test": "jest"
  }
}
